import MarketingPageShell from "@/components/MarketingPageShell";

export default function BrokerServicesPage() {
  return (
    <MarketingPageShell eyebrow="Domains" title="Broker services">
      <p>
        We represent buyers and sellers who need a structured path from interest
        to closed transfer — without exposing strategy in public WHOIS noise or
        auction theater. Outreach, counteroffers, and escrow-aware timelines
        are handled with clear documentation for legal and finance teams.
      </p>
      <p>
        Brokerage can be paired with managed DNS, mail, and application cutover
        so the name moves from handshake to production under one operator
        relationship.
      </p>
    </MarketingPageShell>
  );
}
