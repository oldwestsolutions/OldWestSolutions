import MarketingPageShell from "@/components/MarketingPageShell";

export default function FindADomainPage() {
  return (
    <MarketingPageShell eyebrow="Domains" title="Find a domain">
      <p>
        Whether you are launching a new product line, consolidating a portfolio,
        or securing a defensive name, we help you shortlist, validate, and
        acquire domains that fit your technical and brand constraints — not
        just what is available in a bulk search.
      </p>
      <p>
        Tell us the audience, jurisdictions, and how the name will attach to
        applications, mail, and compliance programs. We align acquisition with
        DNS, TLS, and delivery so the asset is usable on day one, not a parked
        hostname.
      </p>
    </MarketingPageShell>
  );
}
